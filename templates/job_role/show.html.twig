{% extends 'base.html.twig' %}

{% block title %}JobRole{% endblock %}

{% block body %}
    <h1>JobRole</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_role.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ job_role.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_job_role_index') }}">back to list</a>

    <a href="{{ path('app_job_role_edit', {'id': job_role.id}) }}">edit</a>

    {{ include('job_role/_delete_form.html.twig') }}
{% endblock %}
