{% extends 'base.html.twig' %}

{% block title %}JobRole index{% endblock %}

{% block body %}
    <h1>JobRole index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for job_role in job_roles %}
            <tr>
                <td>{{ job_role.id }}</td>
                <td>{{ job_role.title }}</td>
                <td>
                    <a href="{{ path('app_job_role_show', {'id': job_role.id}) }}">show</a>
                    <a href="{{ path('app_job_role_edit', {'id': job_role.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_job_role_new') }}">Create new</a>
{% endblock %}
